const { allPosts, postById, addPost, addToFavorites, deletePost } = require("../../controllers/postsController.js");

describe("addToFavorites", () => {
    test(`passes when value is NaN`, () => {
        expect(NaN).toBeNaN();
        expect(1).net.toBeNaN();
    })
})

describe("deletePost", ()=> {
    test(`passes when value is NaN`, () => {
        expect(NaN).toBeNaN();
        expect(1).net.toBeNaN();
    })
})

describe("addPost", ()=> {
    test(`passes when value is NaN`, () => {
        expect(NaN).toBeNaN();
        expect(1).net.toBeNaN();
    })
})

describe("postById", ()=> {
    test(`passes when value is NaN`, () => {
        expect(NaN).toBeNaN();
        expect(1).net.toBeNaN();
    })
})

describe("allPosts", ()=> {
    test(`passes when value is NaN`, () => {
        expect(NaN).toBeNaN();
        expect(1).net.toBeNaN();
    })
})